# Grade 7 English Review Slides (Unit 1-5)
# 七年级上学期英语复习资料（Unit 1-5）

这是一个基于 React + Vite 构建的交互式英语复习 Web 应用。整合了七年级上学期 Unit 1 到 Unit 5 的核心词汇、短语和句型。

## ✨ 功能特点 (Features)

*   **📚 完整覆盖**：包含 Unit 1-5 所有重点内容。
*   **🔊 离线发音**：内置 370+ 个单词和例句的真人/TTS发音音频（MP3），无需联网即可朗读。
*   **🖼️ 图文并茂**：核心词汇配有精选实景图或生动插画，辅助记忆。
*   **🖱️ 交互学习**：
    *   点击 **Read** 朗读发音。
    *   点击 **Translation** 显示/隐藏中文翻译。
    *   点击 **Explain** 查看详细释义和例句。
    *   扩展知识点支持弹窗交互学习。
*   **📱 响应式设计**：完美支持手机、平板和电脑访问。

## 🚀 快速部署 (Deploy)

你可以通过点击下面的按钮，将本项目一键部署到 Cloudflare Pages。

[![Deploy to Cloudflare Pages](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/YOUR_USERNAME/YOUR_REPO_NAME)

**注意：**
1.  你需要先将本项目代码上传到你自己的 GitHub 仓库。
2.  替换上面链接中的 `YOUR_USERNAME` 和 `YOUR_REPO_NAME` 为你的 GitHub 用户名和仓库名。
3.  Cloudflare Pages 会自动识别 `Vite` 框架。
    *   **Build command:** `npm run build`
    *   **Build output directory:** `dist`

## 🛠️ 本地运行 (Run Locally)

如果你想在本地电脑上运行或修改代码：

1.  **安装依赖**：
    ```bash
    npm install
    # 或者
    pnpm install
    ```

2.  **启动开发服务器**：
    ```bash
    npm run dev
    ```

3.  **构建生产版本**：
    ```bash
    npm run build
    ```
    构建后的文件位于 `dist` 目录。由于浏览器安全策略，直接打开 `dist/index.html` 可能无法正常工作，建议使用 `http-server` 或 VS Code Live Server 预览。

## 📂 目录结构 (Structure)

*   `src/data`: 存放各单元的教学内容数据 (TypeScript)。
*   `src/assets/images`: 存放图片资源。
*   `public/audio`: 存放生成的 MP3 音频文件。
*   `src/components`: React 组件。

## 📝 License

MIT
